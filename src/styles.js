import { createGlobalStyle } from 'styled-components'

const GloblStyle = createGlobalStyle`
body {
    margin: 0;
    padding: 0;
    font-family: 'Poiret One', cursive;
}

* {
    box-sizing: border-box;
}
`;

export default GloblStyle